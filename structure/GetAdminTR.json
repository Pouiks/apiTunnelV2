{
  "modals": {
    "<modalKey: STEP_N__EVENT_NAME>": {
      "step": "number (step du tunnel)",
      "trigger": "string (EXPRESS_BOOKING_CLICK | PAYMENT_STEP_ENTER | DOCUMENTS_STEP_ENTER)",
      "fr": {
        "title": "string",
        "subtitle": "string",
        "sections": [
          {
            "icon": "string (document | shield | home | credit-card | calendar | upload | check-circle)",
            "title": "string",
            "description": "string (peut contenir du HTML inline)",
            "listHeading": "string",
            "listItems": ["string"],
            "infoBox": {
              "icon": "string (lightbulb | clock | check | lock | info)",
              "content": "string"
            }
          }
        ]
      },
      "en": {
        "title": "string",
        "subtitle": "string",
        "sections": ["(meme structure que fr)"]
      }
    }
  },

  "residenceOverrides": {
    "<residenceId: uuid>": {
      "residenceId": "string (uuid)",
      "city": "string",
      "cityAlias": "string",
      "flag": {
        "code": "string (POPULAR | NEW | SPECIAL_OFFER | LAST_UNITS)",
        "label": "string",
        "typologies": "string[] (COLOC | STUDIO | T1_BIS | T2 | T2_DUPLEX | T3) — vide = toutes les typologies heritent"
      },
      "photos": [
        {
          "url": "string (uri)",
          "alt": "string",
          "order": "number",
          "context": "string (HERO | COMMON | TYPOLOGY)",
          "typologyCode": "string | null — present uniquement si context = TYPOLOGY"
        }
      ]
    }
  },

  "steps": [
    {
      "stepCode": "string (CHOOSE_RESIDENCE | CONFIGURE_STAY | PAYMENT | DOCUMENTS | CONFIRMATION)",
      "order": "number",
      "fr": {
        "title": "string",
        "description": "string"
      },
      "en": {
        "title": "string",
        "description": "string"
      }
    }
  ]
}
